import type {Approach,Context} from "@/types"; import {roleRules} from "@/data/lab-data";
const objectiveIntent:Record<string,string>={"Enter a new market":"establish relevance and reduce adoption uncertainty","Launch a new solution":"make the new solution understandable and credible","Generate qualified leads":"create a useful reason for technical dialogue","Support sales":"equip sales with a consistent, relevant conversation","Strengthen market position":"reinforce differentiated market relevance"};
const solutionBenefit:Record<string,string>={"Modular production system":"adaptable production capacity","Industrial automation solution":"reliable, integrated operations","Production technology platform":"a consistent foundation for production development","Technical service solution":"predictable technical support across the lifecycle","Industrial component system":"dependable integration at component level"};
export function buildApproach(c:Context):Approach{const r=roleRules[c.role];const intent=objectiveIntent[c.objective]||"deepen relevance in an established market";const benefit=solutionBenefit[c.solution];return {hypothesis:`In ${c.market}, ${c.industry.toLowerCase()} teams evaluating a ${c.solution.toLowerCase()} may respond to a proposition centred on ${r.lens}. To ${intent}, the message should connect technical substance with the decisions owned by the ${c.role.toLowerCase()}.`,priorities:r.priorities,value:`For ${c.industry} organizations in ${c.market}, the ${c.solution.toLowerCase()} provides a structured route to ${benefit}—giving the ${c.role.toLowerCase()} clearer control over ${r.priorities[0].toLowerCase()} and ${r.priorities[1].toLowerCase()}.`,core:`Turn ${c.solution.toLowerCase()} complexity into ${benefit}.`,support:`A technically grounded approach aligned with ${c.role.toLowerCase()} priorities and the realities of ${c.industry.toLowerCase()}.`,proofs:r.proofs};}
